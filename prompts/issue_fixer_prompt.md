Pick one open GitHub issue in this repo, implement a high‑quality fix, and open a pull request targeting the `dev` branch. Ensure all tests, linters, and other quality gates pass before creating the PR, and then add a `fixed` tag (or equivalent label) to the issue so reviewers know it is ready for review.

